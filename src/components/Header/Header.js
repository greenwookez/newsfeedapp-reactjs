import React from 'react'
import styles from './styles'

const Header = () => {
  return (
    <div style={styles.wrapper}>
      <div style={styles.titleContainer}>
        <p style={styles.title}>NewsFeed App</p>
      </div>
    </div>
  )
}

export default Header
